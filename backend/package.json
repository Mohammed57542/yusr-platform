{
  "name": "yusr-backend",
  "version": "1.0.0",
  "description": "منصة يسر التعليمية - Backend API",
  "type": "module",
  "main": "server.js",
  "scripts": {
    "start": "node --env-file=.env server.js",
    "seed": "node --env-file=.env seed.js",
    "dev": "node --env-file=.env --watch server.js",
    "test": "node --test tests/*.test.mjs"
  },
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "cors": "^2.8.5",
    "express": "^4.19.2",
    "express-rate-limit": "^8.6.2",
    "helmet": "^8.3.0",
    "jsonwebtoken": "^9.0.2"
  }
}
